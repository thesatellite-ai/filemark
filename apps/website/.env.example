# Cloudflare API token with "Edit Cloudflare Workers" template,
# scoped to the aman@khanakia.com account (NOT browser-login,
# which grabs whatever account is signed in). Required by `task deploy:cf`.
CLOUDFLARE_API_TOKEN=
